# Pi-hole Docker Environment Variables

# Pi-hole Version
PIHOLE_VERSION=latest

# Web Interface Password
WEBPASSWORD=change_me

# Timezone
TIMEZONE=UTC

# Server IP (should be static IP of your Docker host)
SERVER_IP=192.168.1.2

# DNS Ports
PIHOLE_DNS_PORT=53

# Web Interface Port
PIHOLE_WEB_PORT=80

# DHCP Port (only if using Pi-hole as DHCP server)
PIHOLE_DHCP_PORT=67

# Upstream DNS Servers
UPSTREAM_DNS_1=1.1.1.1
UPSTREAM_DNS_2=1.0.0.1

# DNS Listening Behavior
# Options: all, local, single
DNSMASQ_LISTENING=local

# Pi-hole DNS (e.g., unbound for recursive DNS)
PIHOLE_DNS=unbound

# Add any additional environment variables below