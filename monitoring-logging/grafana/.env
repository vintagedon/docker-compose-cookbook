# .env file for Grafana Docker setup
# GitHub: https://github.com/vintagedon/docker-compose-cookbook/tree/main/monitoring-logging/Grafana
# Description: This file contains environment variables for configuring the Grafana Docker instance.

# Basic Grafana container configuration
GRAFANA_CONTAINER_NAME=grafana
GRAFANA_CONFIG_DIR=/path/to/your/grafana/config
GRAFANA_DATA_DIR=/path/to/your/grafana/data
GRAFANA_PORT=3000

# Common Grafana configuration options
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
GF_USERS_ALLOW_SIGN_UP=false
GF_AUTH_ANONYMOUS_ENABLED=false

# Advanced Grafana configuration options
GF_SERVER_ROOT_URL=http://localhost:3000
GF_SMTP_ENABLED=false
GF_SMTP_HOST=smtp.example.com:587
GF_SMTP_USER=your-smtp-user
GF_SMTP_PASSWORD=your-smtp-password
GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource

# Note: Replace the placeholder values with your actual configuration details.
# Uncomment and adjust the options as needed for your specific use case.
