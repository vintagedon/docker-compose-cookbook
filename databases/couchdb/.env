# .env file for CouchDB Docker setup
# GitHub: https://github.com/vintagedon/docker-compose-cookbook
# Description: This file contains environment variables for configuring the CouchDB Docker instance.

# CouchDB basic configuration
COUCHDB_CONTAINER_NAME=couchdb
COUCHDB_USER=admin
COUCHDB_PASSWORD=your_secure_password
COUCHDB_SECRET=your_secret_for_cookies
COUCHDB_NODENAME=couchdb@localhost
COUCHDB_PORT=5984

# Directory paths for persistent storage
COUCHDB_DATA_DIR=/path/to/your/couchdb/data
COUCHDB_CONFIG_DIR=/path/to/your/couchdb/config

# CouchDB advanced configuration options (uncomment and modify as needed)

# Set the maximum number of open databases
#COUCHDB_MAX_DBS_OPEN=500

# Set the maximum number of HTTP connections
#COUCHDB_MAX_CONNECTIONS=2048

# Set the session timeout in seconds
#COUCHDB_SESSION_TIMEOUT=600

# Set the OS process timeout in milliseconds
#COUCHDB_OS_PROCESS_TIMEOUT=5000

# Allow persistent cookies (true/false)
#COUCHDB_ALLOW_PERSISTENT_COOKIES=true

# Set the maximum document size in bytes (default is 4GB)
#COUCHDB_MAX_DOCUMENT_SIZE=4294967296

# Note: Replace the placeholder values with your actual configuration details.
# Ensure to use strong, unique values for passwords and secrets in production environments.
# Uncomment and adjust the additional options as needed for your specific use case.
