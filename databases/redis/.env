# .env file for Redis Docker setup
# GitHub: https://github.com/vintagedon/docker-compose-cookbook
# Description: This file contains environment variables for configuring the Redis Docker instance.

# Redis basic configuration
REDIS_CONTAINER_NAME=redis
REDIS_PORT=6379
REDIS_DATA_DIR=/path/to/your/redis/data
REDIS_CONFIG_FILE=/path/to/your/redis/redis.conf

# Redis advanced configuration options (uncomment and modify as needed)

# Set the maximum memory Redis can use
#REDIS_MAXMEMORY=100mb

# Set the max memory policy (allkeys-lru, volatile-lru, allkeys-random, volatile-random, volatile-ttl, noeviction)
#REDIS_MAXMEMORY_POLICY=allkeys-lru

# Additional Redis configuration options (add to redis.conf file)
# Uncomment and set values as needed

# Enable AOF persistence
#appendonly yes

# Set AOF fsync policy (always, everysec, no)
#appendfsync everysec

# Set the number of databases
#databases 16

# Enable Redis authentication
#requirepass your_secure_password

# Set the maximum number of clients
#maxclients 10000

# Enable/disable protected mode
#protected-mode yes

# Note: Replace the placeholder values with your actual configuration details.
# Ensure to use strong, unique values for passwords in production environments.
# Uncomment and adjust the additional options as needed for your specific use case.
