# .env file for SQLite Docker setup
# GitHub: https://github.com/vintagedon/docker-compose-cookbook
# Description: This file contains environment variables for configuring the SQLite Docker instance.

# SQLite basic configuration
SQLITE_CONTAINER_NAME=sqlite_db
SQLITE_DATABASE=your_database.db
SQLITE_DATA_DIR=/path/to/your/sqlite/data

# SQLite Web interface configuration
SQLITE_WEB_CONTAINER_NAME=sqlite_web
SQLITE_WEB_PORT=8080

# SQLite advanced configuration options (uncomment and modify as needed)

# Set the journal mode (DELETE, TRUNCATE, PERSIST, MEMORY, WAL, OFF)
#SQLITE_JOURNAL_MODE=WAL

# Set the synchronous mode (0=OFF, 1=NORMAL, 2=FULL, 3=EXTRA)
#SQLITE_SYNCHRONOUS=NORMAL

# Set the temp store mode (0=DEFAULT, 1=FILE, 2=MEMORY)
#SQLITE_TEMP_STORE=MEMORY

# Set the mmap size (in bytes, 0 to disable)
#SQLITE_MMAP_SIZE=0

# Set the cache size (in pages, negative values for kibibytes)
#SQLITE_CACHE_SIZE=-2000

# Note: Replace the placeholder values with your actual configuration details.
# Uncomment and adjust the additional options as needed for your specific use case.
