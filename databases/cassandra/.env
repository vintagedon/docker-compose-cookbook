# .env file for Apache Cassandra Docker setup
# GitHub: https://github.com/vintagedon/docker-compose-cookbook
# Description: This file contains environment variables for configuring the Cassandra Docker instance.

# Cassandra basic configuration
CASSANDRA_CONTAINER_NAME=cassandra
CASSANDRA_NATIVE_TRANSPORT_PORT=9042
CASSANDRA_RPC_PORT=9160
CASSANDRA_DATA_DIR=/path/to/your/cassandra/data
CASSANDRA_CONFIG_DIR=/path/to/your/cassandra/config

# Cassandra cluster configuration
CASSANDRA_CLUSTER_NAME=MyCluster
CASSANDRA_SEEDS=cassandra
CASSANDRA_NUM_TOKENS=256
CASSANDRA_DC=datacenter1
CASSANDRA_RACK=rack1
CASSANDRA_ENDPOINT_SNITCH=SimpleSnitch

# JVM configuration
CASSANDRA_MAX_HEAP_SIZE=512M
CASSANDRA_HEAP_NEWSIZE=100M

# Additional Cassandra configuration options (uncomment and modify as needed)

# Set the commit log sync mode (periodic or batch)
#CASSANDRA_COMMITLOG_SYNC=periodic

# Set the commit log sync period in milliseconds
#CASSANDRA_COMMITLOG_SYNC_PERIOD_IN_MS=10000

# Set the auto snapshot before major compaction
#CASSANDRA_AUTO_SNAPSHOT=true

# Set the threshold for warning about large partitions during compaction
#CASSANDRA_COMPACTION_LARGE_PARTITION_WARNING_THRESHOLD_MB=100

# Set the memtable allocation type (heap_buffers or offheap_buffers)
#CASSANDRA_MEMTABLE_ALLOCATION_TYPE=heap_buffers

# Set the compaction throughput in MB/s
#CASSANDRA_COMPACTION_THROUGHPUT_MB_PER_SEC=16

# Set the stream throughput outbound in MB/s
#CASSANDRA_STREAM_THROUGHPUT_OUTBOUND_MEGABITS_PER_SEC=200

# Set the inter-node communication timeout in ms
#CASSANDRA_INTERNODE_SEND_RECV_TIMEOUT_IN_MS=10000

# Note: Replace the placeholder values with your actual configuration details.
# Uncomment and adjust the additional options as needed for your specific use case.
