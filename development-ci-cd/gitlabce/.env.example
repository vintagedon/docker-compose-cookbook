# GitLab CE Docker Environment Variables

# GitLab Version
GITLAB_VERSION=latest

# Container Name
CONTAINER_NAME=gitlab

# GitLab Configuration
GITLAB_HOSTNAME=localhost
GITLAB_EXTERNAL_URL=http://localhost:8080

# Ports
HTTP_PORT=8080
HTTPS_PORT=8443
SSH_PORT=2222

# GitLab Runner Configuration (if using GitLab Runner)
# GITLAB_RUNNER_TOKEN=your_runner_token_here

# SMTP Configuration (for email notifications)
# SMTP_ENABLED=true
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password
# SMTP_DOMAIN=example.com
# SMTP_AUTHENTICATION=login
# SMTP_STARTTLS_AUTO=true

# LDAP Configuration (if using LDAP)
# LDAP_ENABLED=true
# LDAP_HOST=ldap.example.com
# LDAP_PORT=389
# LDAP_UID=uid
# LDAP_METHOD=plain
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_PASSWORD=admin_password
# LDAP_BASE=dc=example,dc=com

# Backup Configuration
# GITLAB_BACKUP_SCHEDULE=daily
# GITLAB_BACKUP_TIME=01:00

# Add any additional environment variables below